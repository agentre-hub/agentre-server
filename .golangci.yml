version: "2"
run:
  timeout: 10m
linters:
  default: standard
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - misspell
issues:
  exclude-rules:
    - path: _test\.go
      linters: [errcheck]

# 构建上下文是仓库根目录（Dockerfile 在 deploy/），所以这个文件留在根上。
# 少了它上下文有 236MB，且宿主机的 frontend/node_modules 会被 COPY 进去，
# pnpm install 想清掉它，在无 TTY 的构建环境里直接失败。

.git
.gitignore
.github
.gitea
.worktrees
.dev-kit

**/node_modules
frontend/dist
internal/web/dist
bin
*.tsbuildinfo

runtime
data
coverage.out
coverage.html

# 真实凭据，绝不进任何一层镜像
.env
configs/config.yaml

e2e
docs
deploy
README.md
AGENTS.md
CLAUDE.md

.idea
.vscode
.omc
.opskat
*.swp
.DS_Store
